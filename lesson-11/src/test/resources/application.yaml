spring:
  datasource:
    url: jdbc:tc:postgresql:16://localhost/test?TC_DAEMON=true
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

